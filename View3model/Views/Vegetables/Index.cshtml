@model IEnumerable<View3model.Models.Vegetable>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VegetableName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VegetableName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VegetableId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VegetableId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VegetableId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
