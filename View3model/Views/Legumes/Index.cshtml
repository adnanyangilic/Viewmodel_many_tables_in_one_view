@model IEnumerable<View3model.Models.Legume>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LegumeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LegumeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LegumeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LegumeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LegumeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
